# NetPay Calc 技术方案文档

## 技术架构

- Vite 5 + React 18 + TypeScript 5，纯客户端 SPA。
- Tailwind CSS 3.4 负责响应式界面。
- `src/utils/calculator.ts` 提供纯函数计算能力（含完整类型定义）。
- `src/components/Calculator.tsx` 管理输入状态与结果展示。
- Hash-based 路由（`#/`、`#/login`、`#/dashboard`），无需服务端 fallback。
- Dashboard 使用 localStorage 保存用户历史记录。
- 环境变量使用 `VITE_` 前缀，通过 `import.meta.env` 访问。
- 部署至 EdgeOne Pages，构建命令 `npm run build`，输出目录 `dist`。
- 单元测试使用 Vitest（`npm test`），覆盖计算引擎核心逻辑。

## 每月额外收入模型

前端维护固定 12 项数组：

```text
monthlyExtraIncomes[month - 1] = {
  taxable: number | string,
  nonTaxable: number | string
}
```

计算入口 `calculateNetPay` 新增：

- `monthlyExtraIncomes`：12 个月的计税、不计税额外收入。
- `selectedMonth`：当前展示月份，范围 1—12。

缺失、非法或负数额外收入统一按 0 处理。`calculateNetPay` 返回：

- `annual.monthlyExtraIncomes`：标准化后的 12 个月额外收入。
- `annual.monthlyResults`：12 个月完整计算结果，包含总收入、两类额外收入、五险一金、个税和到手收入，供月份卡片直接渲染。

## 年终奖模型

主计算器支持全年一次性年终奖，默认计入 12 月发放（`yearEndBonus` 参数），提供两种计税口径（`bonusTaxMode`）：

- `combined`（并入综合所得）：年终奖计入 12 月月度增量，随累计预扣法计税。
- `separate`（单独计税）：年终奖 ÷ 12 查月度换算税率表（`MONTHLY_TAX_BRACKETS`）确定税率与速算扣除数，税额 = 年终奖 × 税率 − 速算扣除数，不进入工资累计预扣。

计算口径：

```text
并入模式：12 月增量 = 基础增量 + 12 月计税额外收入 + 年终奖
单独模式：12 月个税 = 当月累计预扣税额 + 年终奖单独计税税额
年度个税 = 累计应预扣税额 + 年终奖单独计税税额（并入模式该项为 0）
```

`MonthResult` 新增 `yearEndBonus`（计入本月的年终奖，仅 12 月非 0）与 `bonusTax`（单独计税税额）；`CalcResult.annual` 新增 `yearEndBonus`、`bonusTax` 汇总字段。非法或负值年终奖统一按 0 处理。`calculateBonusTaxSeparate` 为纯函数，可独立测试。

## 计算规则

每月五险一金仍以基础月薪和城市缴费上下限计算，额外收入不改变缴费基数。

```text
月度应纳税所得额增量 = 基础月薪 + 当月计税额外收入 - 五险一金 - 5000 - 专项附加扣除
累计应纳税所得额 = 过去各月增量之和（增量允许为负，扣除缺口可结转抵扣）
累计应预扣税额 = max(0, max(0, 累计应纳税所得额) × 适用税率 - 速算扣除数)
当月预扣税额 = 本月累计应预扣税额 - 上月累计已预扣税额

当月总收入 = 基础月薪 + 计税额外收入 + 不计税额外收入
当月到手收入 = 当月总收入 - 五险一金 - 当月预扣税额
```

`calculateAnnualTaxCumulative` 接收月度增量数组（允许为负），内部先累加、再对累计值取 0 下限后查税率表。低薪月份未用满的 5000 起征额度和专项附加扣除会结转到后续月份，抵扣年终奖等一次性收入后再计税。`MonthResult.cumulativeTaxableIncome` 与 `monthlyBreakdown` 记录各月末累计应纳税所得额。

年度个税为 12 月末的累计应预扣税额。年度到手收入包含全年基础工资、两类额外收入，并扣除全年五险一金和年度个税。

## 快捷应用与响应式布局

- 基础工资是全年统一输入，修改后自动影响 12 个月。
- `applyExtraIncomeToAllMonths(field)` 可将当前月某一类额外收入复制到全年，并保留另一类字段。
- 不传 `field` 时，将当前月两类额外收入同时复制到全年；清空操作重建 12 项空数组。
- 月份卡片直接消费 `annual.monthlyResults`，避免 UI 重复实现税后计算公式。
- 网格断点为默认单列、`sm` 双列、`lg` 三列、`xl` 四列；首页及 Dashboard 使用 `max-w-7xl` 提供平铺空间。

## 城市配置与政策数据

- 城市缴费基数上下限与公积金默认比例统一维护在 `calculator.ts` 的 `CITY_LIMITS`，UI 通过 `getCityHousingRate(city)` 读取，不再在组件中内联城市比例表（单一数据源）。
- `POLICY_DATA_YEAR` 常量（当前 2024）标注个税税率表、社保缴费基数、专项附加扣除标准的适用年度；结果区税务注释同步展示，便于年度政策更新时定位修改点。
- 年终奖单独计税政策依据财税〔2018〕164 号，现行有效期延续至 2027 年 12 月 31 日。

## 输入校验与数据治理

- 社保公积金比例输入通过 `clampNumber` 钳制：养老 0-20%、医疗 0-10%、失业 0-2%、公积金 0-24%；非法或非数字输入回退到下限，避免 NaN 结果。
- 历史记录容量上限 `MAX_HISTORY_ENTRIES = 100`：超出时保留最新记录并提示「最早的记录已自动移除」；localStorage 写入失败（空间不足）时提示用户清理。
- 加载历史记录时同样按上限截断，兼容旧数据超限场景。

## 视觉系统

- 全局背景使用 `slate-950`，叠加低透明度 emerald/blue 径向渐变，形成克制的金融看板氛围。
- 内容面板统一使用 `dashboard-panel`：白色背景、`slate-200` 边框、中等圆角和深色柔和阴影。
- 主色使用 emerald：主操作、收入、当前月份状态；支出与扣除使用 red；提示使用 amber。
- 通用输入和按钮通过 `.input`、`.btn-primary`、`.btn-secondary` 统一焦点态与最小触控高度。
- 月份卡片直接消费 `annual.monthlyResults`，选中卡片使用深色底和绿色状态标识。
- 主计算器社保公积金设置：
  - 可折叠面板展示四项个人缴费比例（养老、医疗、失业、公积金）。
  - 默认值：养老 8%、医疗 2%、失业 0.5%、公积金按城市默认。
  - 切换城市时自动更新公积金默认值，其他比例保持不变。
  - `customRates` 参数扩展为 `{ pension?, medical?, unemployment?, housing? }`。
  - `calculateInsurance` 函数签名同步更新，支持全部四项个人缴费比例。
- 未引入图片或图标依赖，品牌标识使用文本字符 `¥`，避免无业务价值的媒体资源增加加载成本。

## 涨薪对比模块

- 独立组件 `SalaryIncreaseCalculator`，以全屏浮层形式展示，不依赖主计算器当前结果。
- 仅按税前口径对比，不计算五险一金与个税，避免缴费基数与扣除口径差异干扰跳槽决策。
- `JobConfig` 接口定义一份工作的配置：`{ salary, bonus }`（税前月薪、年终奖）。
- `calcJob(job: JobConfig)` 纯函数计算单份工作的税前数据：年度总包 = 月薪 × 12 + 年终奖，平均月税前 = 年度总包 ÷ 12。
- 对比表格四列：对比项、上一份工作、当前工作、涨幅；涨幅为负时红色标识。
- 对比维度：税前月薪、年终奖、年度总包、平均月税前。
- 汇总卡片展示年度总包涨幅和税前月薪涨幅（百分比与金额差）。
- 年终奖为空或负数按 0 处理；上一份工作年终奖为 0 时该行涨幅显示「—」。
- 浮层实现键盘无障碍：`role="dialog"`、`aria-modal`、`aria-labelledby`、ESC 关闭；Tab 焦点陷阱使焦点在对话框内循环，打开时自动聚焦面板。

## 数据兼容

历史记录新增：

- `month`
- `baseSalary`
- `taxableExtraIncome`
- `nonTaxableExtraIncome`

旧记录没有上述字段时继续按原有 `gross`、`netPay`、`insurance` 和 `tax` 展示，不执行破坏性迁移。
