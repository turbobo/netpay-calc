# NetPay Calc 技术方案文档

## 技术架构

- Next.js 14 App Router + React 18。
- Tailwind CSS 负责响应式界面。
- `lib/calculator.js` 提供纯函数计算能力。
- `components/Calculator.js` 管理输入状态与结果展示。
- Dashboard 使用 localStorage 保存用户历史记录。

## 每月额外收入模型

前端维护固定 12 项数组：

```text
monthlyExtraIncomes[month - 1] = {
  taxable: number | string,
  nonTaxable: number | string
}
```

计算入口 `calculateNetPay` 新增：

- `monthlyExtraIncomes`：12 个月的计税、不计税额外收入。
- `selectedMonth`：当前展示月份，范围 1—12。

缺失、非法或负数额外收入统一按 0 处理。

## 计算规则

每月五险一金仍以基础月薪和城市缴费上下限计算，额外收入不改变缴费基数。

```text
当月应纳税所得额 = max(
  0,
  基础月薪 + 当月计税额外收入 - 五险一金 - 5000 - 专项附加扣除
)

当月总收入 = 基础月薪 + 计税额外收入 + 不计税额外收入
当月到手收入 = 当月总收入 - 五险一金 - 当月累计预扣税额
```

年度个税按 12 个月不同的应纳税所得额逐月累计计算。年度到手收入包含全年基础工资、两类额外收入，并扣除全年五险一金和累计个税。

## 数据兼容

历史记录新增：

- `month`
- `baseSalary`
- `taxableExtraIncome`
- `nonTaxableExtraIncome`

旧记录没有上述字段时继续按原有 `gross`、`netPay`、`insurance` 和 `tax` 展示，不执行破坏性迁移。
